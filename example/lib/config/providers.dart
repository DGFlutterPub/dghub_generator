/*import 'package:dghub_generator/dghub_generator.dart';
import 'package:example/system/theme/theme_provider.dart';

class Providers {
  ThemeProvider themeProvider(WidgetRef ref) =>
      ref.read(NotifierProvider(() => ThemeProvider()).notifier);
}*/
